# Stock Market Prediction using LSTM and GRU

**Module:** Artificial Intelligence and Neural Networks (COM7019) — Assignment [3921]
**Author:** Farid Negahbani (Student ID: 24154844)
**Institution:** Arden University
**Module Leader:** Ali Vaisifard

## 1. Project Overview

This repository contains an end-to-end deep learning pipeline that designs, trains, and critically compares two recurrent neural network (RNN) architectures — **Long Short-Term Memory (LSTM)** and **Gated Recurrent Unit (GRU)** — for univariate stock closing-price prediction using historical daily OHLCV (Open, High, Low, Close, Volume) time-series data.

The project was developed to satisfy Task 1 of the COM7019 assignment brief: *"Stock Market Prediction using LSTM and GRU"*, which requires the design, implementation, and critical comparison of both architectures, including experimentation with model configurations and the application of techniques to improve generalisation (dropout and early stopping).

## 2. Repository Structure

```
.
├── stock_lstm_gru_prediction.py     # Single-file end-to-end pipeline (all code)
├── data/
│   └── Stock_Price_Data.csv         # Provided daily OHLCV dataset (1980–2020)
├── outputs/                         # Generated automatically when the script is run
│   ├── fig1_closing_price_trend.png
│   ├── fig2_trading_volume_trend.png
│   ├── fig3_feature_correlation_heatmap.png
│   ├── fig4_lstm_loss_curve.png
│   ├── fig5_gru_loss_curve.png
│   ├── fig6_lstm_actual_vs_predicted.png
│   ├── fig7_gru_actual_vs_predicted.png
│   ├── fig8_lstm_vs_gru_comparison.png
│   ├── fig9_model_comparison_metrics.png
│   ├── fig10_hyperparameter_experiment.png
│   ├── model_comparison_results.csv
│   ├── hyperparameter_experiment_results.csv
│   └── summary_report.txt
└── README.md
```

## 3. Dataset

The dataset (`Stock_Price_Data.csv`) consists of **9,909 daily trading records** spanning **12 December 1980 to 1 April 2020**, with the following columns: `Date`, `Open`, `High`, `Low`, `Close`, `Adj Close`, `Volume`. No missing values were present. The `Close` price was selected as the prediction target.

## 4. Methodology

1. **Exploratory Data Analysis (EDA):** visual inspection of the closing-price trend, trading volume, and correlation between OHLCV features.
2. **Pre-processing:** chronological 80/20 train-test split (performed *before* scaling to avoid data leakage), `MinMaxScaler` normalisation, and conversion into supervised sequences using a 60-day sliding window.
3. **Model design:** two stacked recurrent architectures (LSTM and GRU), each with two recurrent layers, `Dropout` regularisation, and two dense layers.
4. **Training:** Adam optimiser, mean-squared-error loss, `EarlyStopping` (patience = 6) to prevent overfitting and restore the best validation weights.
5. **Evaluation:** MAE, RMSE, MAPE, and R² computed on the held-out chronological test set.
6. **Hyperparameter experiment:** a controlled comparison of three LSTM configurations (varying hidden units and dropout rate) to demonstrate the effect of architectural choices on performance.

## 5. How to Run

### Requirements
- Python 3.10+
- `tensorflow` (or `tensorflow-cpu`) >= 2.15
- `pandas`, `numpy`, `scikit-learn`, `matplotlib`

### Installation
```bash
pip install tensorflow-cpu pandas numpy scikit-learn matplotlib
```

### Execution
```bash
python stock_lstm_gru_prediction.py
```
The script reads `data/Stock_Price_Data.csv`, trains both models, and writes all figures, tables, and a text summary to the `outputs/` folder. Total runtime is approximately 8–10 minutes on a standard CPU.

## 6. Results Summary

| Model | MAE | RMSE | MAPE (%) | R² | Training Time (s) | Epochs Run (early-stopped) |
|-------|------|------|----------|-----|--------------------|------------------------------|
| LSTM  | 29.46 | 42.94 | 17.07 | 0.447 | 146.5 | 14 |
| GRU   | 10.70 | 17.16 | 6.07  | 0.912 | 240.4 | 12 |

On this dataset, the **GRU model substantially outperformed the LSTM model** across every error metric while requiring fewer recurrent parameters, indicating it generalised more effectively to the held-out, more volatile (2017–2020) portion of the series. Full discussion, justification, and critical evaluation are provided in the accompanying written report.

## 7. Ethical and Practical Considerations

This project is for **academic purposes only**. Predictions generated by these models must **not** be used for real financial trading or investment decisions. Stock markets are influenced by a vast range of unmodelled exogenous factors (macroeconomic news, geopolitical events, investor sentiment), and a univariate technical model of this kind carries no guarantee of future predictive accuracy. See the written report for a full discussion of ethical implications and mitigations (Learning Outcomes 3 and 4).

## 8. License

This repository is submitted as part of an academic assessment for Arden University (COM7019) and is not licensed for commercial reuse.
